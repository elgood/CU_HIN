#!/usr/bin/env python3

import argparse
import whois
import time
import numpy as np

#take data
def processNetflow(netflowInput):
    #pull collum with dns


#when dns is found run whois
def whoisprocess(dns):

#from whois take domain registry domain info

#pass to domain registry_domain.py george's code

def menu_selection():
    netflowInput = input('Enter the file name: ')


if __name__ == "__main__":
    #menu selection
    menu_selection()